import { test } from "@playwright/test";

test.describe.skip("reconnect resync — requires frontend runtime", () => {
  test("client receives current snapshot on reconnect", () => {});
  test("snapshot version matches authoritative state", () => {});
});
