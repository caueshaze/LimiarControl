import { test } from "@playwright/test";

test.describe.skip("targeting resolution — requires frontend runtime", () => {
  test("sphere shape covers expected cells", () => {});
  test("cone respects obstacle blocking", () => {});
});
